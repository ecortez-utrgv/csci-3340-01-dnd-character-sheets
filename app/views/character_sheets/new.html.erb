<% content_for :title, "New character sheet" %>

<h1 class="font-bold text-4xl mbe-4">New character sheet</h1>

<%= render "form", character_sheet: @character_sheet %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to character sheets", character_sheets_path, class: "btn" %>
</div>
